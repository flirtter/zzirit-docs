---
date: 2026-03-19
type: handover
session_id: s2
---

## 이번 세션 요약

### 1. MY 탭 Figma 디자인 매칭 ✅
- **보유 볼트**: flat row → 노란 라운드 카드 (2줄 레이아웃 + 검정 충전 버튼)
- **Like 카드**: 52px 썸네일 → 140x180 큰 카드 + 이름/뱃지 오버레이 + More 카드
- **아이콘**: 컬러 outline → 검정 filled (heart, location, pencil)
- **칩 버튼**: 회색 배경 → 흰 배경 + border
- **완성도 뱃지**: 아바타 아래 중앙 → 좌하단 오버랩
- **프로필~콘텐츠 구분선**: 두꺼운 회색 밴드 제거

### 2. 채팅방 생성 + 라우팅 ✅
- `services/chatService.ts` 신규 생성: `createOrGetChatRoom()`, `initiateChatWithBolt()`
- 3곳(번개탭, 미팅상세, 프로필상세)에서 볼트 차감 → Firestore 채팅방 생성 → chattingroom 라우팅
- 기존: 볼트 차감만 되고 Alert만 표시 → 수정: 실제 채팅방 생성 + 화면 이동
- **시뮬레이터 검증**: 데모유저1과 채팅방 생성 성공, 메시지 전송 확인

### 3. Like 테스트 데이터 생성
- user_01~05 → 현재 유저(YativJyC3hR2SAU6Kq5R5ArgU33)로 좋아요 5건 생성
- Firestore에 정상 저장 확인
- **미해결**: 앱이 `localhost:8080` 대신 프로덕션 API에 연결하는 것으로 확인됨. 로컬 API에서는 5건 반환되지만 앱에서는 0건. `__DEV__=true`인데도 프로덕션 URL 사용 중 — Metro 캐시 또는 번들 이슈로 추정

## 변경된 파일

### zzirit-rn (수정 4 + 신규 1)
- `app/(tabs)/my.tsx` — Figma 디자인 매칭 (볼트카드, Like카드, 아이콘, 칩버튼)
- `app/(tabs)/index.tsx` — 번개탭 채팅 확인 → chatService 연동
- `app/meeting-detail.tsx` — 미팅상세 채팅 확인 → chatService 연동
- `app/profile-detail.tsx` — 프로필상세 채팅 확인 → chatService 연동
- `services/chatService.ts` — (신규) 채팅방 생성/조회 + 볼트차감 유틸

### zzirit-api (변경 없음 — 디버그 코드 추가/제거 후 원복)

## 미완료 TODO

- [ ] MY 탭 Like 카드 실데이터 표시 — 앱이 프로덕션 API에서 0건 반환, Metro 캐시 클리어 또는 `services/api.ts`의 BASE_URL 확인 필요
- [ ] 채팅방 "Unknown User" → 상대방 이름 표시 (ChatContext의 partnerName 로직 개선)
- [ ] 채팅방에서 뒤로가기 후 이전 화면으로 정상 복귀 확인
- [ ] 매칭 알림 + 매칭 목록
- [ ] 번개탭 실시간 위치 업데이트 (현재 10분 주기)
- [ ] 프로필 편집 → Firestore 반영
- [ ] 디자인 파리티 검수 (나머지 surface: 번개, 미팅, 채팅)

## 다음 세션 추천 작업

1. **Like 0건 이슈 해결** — `npx expo start --clear` 로 Metro 캐시 클리어 후 재확인. `services/api.ts`에서 `console.log(BASE_URL)` 추가하여 실제 호출 URL 확인
2. **채팅방 UX 개선** — Unknown User 이름 표시, 뒤로가기 동작 확인
3. **프로필 편집 Firestore 반영** — my-edit.tsx에서 저장 시 Firestore 업데이트

## 기술 메모

- 현재 유저 UID: `YativJyC3hR2SAU6Kq5R5ArgU33`
- Firestore likes 데이터: 5건 확인 (user_01~05 → 현재 유저)
- 앱 API 연결: `__DEV__=true`이지만 실제로는 프로덕션 API 사용 중 (로컬 API 로그에 앱 요청 없음)
- 볼트 잔액: 10,090 (10,100 - 10 채팅 차감)
- 시뮬레이터: iPhone 16 Pro (iOS 18.3), Metro 8081
