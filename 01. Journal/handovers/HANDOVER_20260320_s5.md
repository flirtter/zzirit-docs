---
date: 2026-03-20
type: handover
session_id: s5
---

## 이번 세션 요약

### 1. 프로필 수정(my-edit) 전면 재구현 ✅
- 사진 5슬롯 (기존 4 → 5, 둥근 사각형, 대표 배지)
- `imagePickerService` 연동 (`allowsEditing: false`) — iOS 시스템 크롭 UI 제거
- `PhotoConfirmModal` 신규 — Figma MY-1 사진 확인 화면 ("다시찍기"/"사진적용")
- 15개 프로필 필드 (기존 7개 + 키/체형/MBTI/주종/종교/연애스타일/이상형/키워드)
- 온보딩 StepProfileDetails 동일 옵션 데이터 사용
- Firestore `profile` 컬렉션 읽기/쓰기 통합
- 뒤로가기 → MY탭 (`router.replace`)

### 2. 채팅 위치 프리뷰 수정 ✅
- Static Map API (iOS 헤더 미지원) → NaverMapView 인라인 렌더링
- `pointerEvents="none"` + 모든 제스처 비활성화
- 스냅샷 캐싱 최적화: 첫 렌더 후 `screenToImage()` → 디스크 캐시 → 이후 `<Image>`로 교체
- 위치 메시지 전체 너비 렌더링 (버블 maxWidth 제약 탈출)
- 메시지 풍선 우측 정렬 통일 (`bubbleColumnRight: alignItems: flex-end`)

### 3. 채팅 Unknown User → 상대방 이름 ✅
- `ChatContext.tsx`: `onSnapshot` 콜백에서 파트너 프로필 Firestore 조회
- 프로필 캐시 (`profileCacheRef`) 추가 — 매 스냅샷 재조회 방지
- `chattingroom.tsx`: route params 없을 때 chatData fallback

### 4. 프로필 상세(profile-detail) 전면 재구현 ✅
- Figma 매칭: 나의 프로필/내 성격/내 취미/이상형/연애스타일 섹션
- 두 Firestore 컬렉션(`profile` + `users`) 병렬 조회 (`Promise.all`)
- 액션 버튼: X(닫기), 하트(찌릿), 보내기(채팅) — 사진 위 오버레이
- API 실패 시 데모 모드 알림 처리

### 5. 더미 유저 10명 + Like 데이터 ✅
- `services/demoData.ts`: 10명 프로필 + 8건 받은 Like + 5건 보낸 Like + 2건 매칭
- `scripts/seedDemoData.ts`: Firestore 시딩 스크립트 (설정 화면에서 실행 가능)
- `likes.tsx`, `my.tsx`: API 빈 응답/실패 시 데모 데이터 폴백

### 6. Like 블러 + 볼트 결제 시스템 ✅
- MY 홈 Like 카드: `blurRadius={likeUnlocked ? 0 : 25}` + 이름 블러 바
- Like 화면: 기존 블러/결제 로직 + AsyncStorage 결제 상태 영속화
- 결제 상태 공유: `AsyncStorage('like_view_purchased')` → 상수화 (`STORAGE_KEYS`)
- MY 홈 ↔ Like 화면 양방향 동기화

### 7. UI 개선 ✅
- `UserPreviewCard`: 액션 버튼 (X/하트/보내기) 추가
- `BottomTabBar`: Firestore에서 유저 프로필 이미지 자체 로드 + 아이콘 크기/가시성 개선
- `LightningProfileCard`: 카드 높이 축소 (SCREEN_HEIGHT - 100) → 다음 유저 peek
- `bolt-charge.tsx`: 이미지 영역 고정 크기 수정

### 8. Simplify 코드 리뷰 ✅
- 데모 데이터 매핑 중복 → `toThumbnails` 헬퍼 추출
- BottomTabBar 불필요한 prop 제거
- AsyncStorage 키 상수화 (`constants/storageKeys.ts`)
- profile-detail 두 Firestore 읽기 `Promise.all` 병렬화
- ChatContext 파트너 프로필 캐시 추가

### 9. Docs 전체 재정리 ✅
- `TODO.md` 신규 — 로드맵 (다음 우선순위, 기술 부채, QA 인프라)
- `references/qa-workflow.md` 신규 — QA 워크플로 결정 (GitHub Issue + Vault MD 하이브리드)
- `current-state.md`, `surface-status.md` 전면 갱신
- `02. Project.md` 칸반 갱신
- `my.md`, `chat.md`, `likes.md` surface contract 갱신

## 변경 파일

### zzirit-rn — 11파일 변경 (+1506, -980)
- `app/(tabs)/my-edit.tsx` — 전면 재작성 (15 필드 + PhotoConfirmModal)
- `app/(tabs)/my.tsx` — Like 블러/데모 폴백/AsyncStorage
- `app/chattingroom.tsx` — NaverMapView 위치 프리뷰 + 정렬 + FileSystem 캐시
- `app/profile-detail.tsx` — 전면 재구현 (프로필 섹션 + 병렬 Firestore)
- `app/likes.tsx` — 데모 폴백 + AsyncStorage 결제 상태
- `app/bolt-charge.tsx` — 이미지 크기 수정
- `app/settings.tsx` — 데모 시드 버튼 추가
- `components/PhotoConfirmModal.tsx` — (신규) Figma MY-1 사진 확인
- `components/UserPreviewCard.tsx` — 액션 버튼 추가
- `components/BottomTabBar.tsx` — Firestore 프로필 이미지 + 아이콘 개선
- `components/lightning/LightningProfileCard.tsx` — 카드 높이 조정
- `contexts/ChatContext.tsx` — 파트너 프로필 조회 + 캐시
- `constants/storageKeys.ts` — (신규) AsyncStorage 키 상수
- `services/demoData.ts` — (신규) 더미 유저/좋아요 데이터
- `scripts/seedDemoData.ts` — (신규) Firestore 시딩 스크립트

### zzirit-docs — 8파일 변경/생성
- `TODO.md` — (신규) 로드맵
- `references/qa-workflow.md` — (신규) QA 워크플로 결정
- `snapshots/current-state.md` — 전면 갱신
- `snapshots/surface-status.md` — 갱신
- `02. Project/02. Project.md` — 칸반 갱신
- `02. Project/my/my.md` — spec_status stable, 15 필드 문서화
- `02. Project/chat/chat.md` — 위치/Unknown User 해결 기록
- `02. Project/likes/likes.md` — 블러/결제 시스템 기록

## 미완료 TODO

- [ ] **프로필 스크롤 피드 완성** — 번개탭 Figma 매칭 (연속 스크롤 + 다음 유저 peek)
- [ ] **커스텀 카메라 UI** — Figma MY.png (그리드 오버레이, 셔터, 비디오/사진 전환)
- [ ] **Like 서버 연동** — 실 API (현재 데모 데이터 폴백)
- [ ] **매칭 알림 + 매칭 목록**
- [ ] 커스텀 사진 크롭 UI (Figma "취소"/"완료" 스타일)
- [ ] 나머지 surface에 imagePickerService 적용 (create-meeting, onboarding)
- [ ] 번개탭 실시간 위치 업데이트 (현재 10분 주기)
- [ ] createOrGetChatRoom O(n) → pair_key 인덱스 (스키마 변경)
- [ ] console.log __DEV__ 게이팅
- [ ] 칩 컴포넌트 공유 모듈 추출 (my-edit ↔ onboarding)
- [ ] StaticMapPreview → 서버 프록시 기반 Static Map 전환 (프로덕션)
- [ ] vault-health CI 설정 (고아 노트, 깨진 링크 자동 감지)

## 다음 세션 추천 작업

1. **프로필 스크롤 피드 완성** — LightningProfileCard가 Figma처럼 연속 스크롤 + 다음 카드 헤더 peek
2. **Like 서버 연동** — zzirit-api에 like 엔드포인트 구현, 데모 폴백 제거
3. **커스텀 카메라 UI** — expo-camera 기반, Figma MY.png 매칭
4. **vault-health CI** — .github/workflows/vault-health.yml 추가

## 기술 메모

- 현재 유저 UID: `YativJyC3hR2SAU6rKq5R5ArgU33`
- 볼트 잔액: 10,090
- 시뮬레이터: iPhone 16 Pro (iOS 18.3), Metro 8081
- 더미 유저: `demo_user_1` ~ `demo_user_10` (Firestore profile + users 컬렉션)
- AsyncStorage 키: `STORAGE_KEYS.LIKE_VIEW_PURCHASED` in `constants/storageKeys.ts`
- NaverMapView 위치 프리뷰: 캐시 경로 `FileSystem.cacheDirectory/map_{lat}_{lng}.jpg`
