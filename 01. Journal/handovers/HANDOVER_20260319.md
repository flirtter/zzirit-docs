# Handover: 2026-03-19 세션

## 현재 상태

### 앱 (zzirit-rn) — main 브랜치
- **빌드**: iPhone 16 Pro 시뮬레이터 (iOS 18.3) 정상 실행
- **인증**: aa@aa.aa → 익명 로그인 바이패스, 온보딩 위치권한 이미 허용 시 자동 스킵
- **배포**: Expo + Metro 8081

### API (zzirit-api) — master 브랜치, Cloud Run 배포 완료
- **리비전**: zzirit-api-00040-z6k
- **미팅 API**: 작성자 프로필(이름/이미지) 조인 반환
- **볼트**: 두 유저 모두 10,000+ 볼트 보유

## 이번 세션 작업 내역

### 핵심 기능
1. **미팅 이미지 업로드** — ImagePicker → Firebase Storage → image_url 저장/표시
2. **프로필 "?" → 실제 유저** — 16개 풍부한 프로필 생성 (이름/이미지/MBTI/성격/취미/이상형), 미팅 작성자 다양화
3. **좋아요/채팅 API 연결** — 번개탭+프로필 상세+미팅 상세 모두 실제 API 호출 (`sendLike`, `bolt/use`)
4. **채팅 볼트 차감** — 미팅/번개/프로필에서 채팅 시 10볼트 차감 확인 모달

### UI/UX
5. **프로필 상세 Figma 매칭** — Ionicons 아이콘, 섹션 카드, 이상형 핑크 칩, 사진 4:5 비율
6. **ProfileCardView 공통 컴포넌트** — profile-detail + LightningProfileCard 통합 (중복 ~200줄 제거)
7. **번개탭 3단 바텀시트** — collapsed/peek/expanded + 지도보기/목록보기 토글 버튼
8. **번개탭 지도** — 줌/나침반/스케일바 숨김, absoluteFill 전체 화면
9. **날짜/시간 피커** — 커스텀 ScrollView → iOS 네이티브 DateTimePicker spinner

### 인프라/데이터
10. **Firestore 시드 데이터** — 16개 프로필 + 16개 location (서울 주요 지역)
11. **Firebase Storage 규칙** — `allow write: if request.auth != null` 배포
12. **config/api.ts** — 프로덕션 URL zzirit-proxy → zzirit-api 수정
13. **naverMapService** — Firebase Auth 인증 헤더 추가

### 코드 품질
14. **useDownloadUrl 전면 재작성** — HTTP URL 직접 반환, 10분 캐시 (기존 300줄 → 50줄)
15. **profileUtils.ts** — `resolveProfileImage()`, `calcKoreanAge()` 유틸 추출
16. **simplify 리뷰** — 거리 계산 O(N²logN)→O(NlogN), React.memo, SPRING_CONFIG 상수
17. **미팅 정렬 버그** — useEffect deps에 meetings 누락 수정

### 버그 수정
18. **profile-detail 훅 순서 에러** — useMemo를 early return 전으로 이동
19. **increment_view 500 에러** — 문서 존재 확인 추가
20. **온보딩 위치 스킵** — 이미 허용 시 자동 다음 스텝

## 알려진 이슈
- **Error getting location**: 시뮬레이터 한정 (실기기 OK)
- **번개탭 이미지**: demo_user 3명은 randomuser.me, user_01~12도 randomuser.me — 실제 사진 필요
- **채팅방 생성**: 볼트 차감만 되고 실제 채팅방 라우팅 미구현

## 다음 세션 TODO
1. 채팅방 실제 생성 + 라우팅 (볼트 차감 후)
2. MY 탭에서 좋아요 보낸/받은 목록 실데이터 표시
3. 매칭 알림 + 매칭 목록
4. 번개탭 실시간 위치 업데이트 (현재 10분 주기)
5. 프로필 편집 → Firestore 반영
6. 디자인 파리티 검수 (Figma ↔ 실제 캡처 대조)

## 기술 메모
- 시뮬레이터 로그 모니터링: `xcrun simctl spawn booted log stream --predicate 'subsystem == "com.facebook.react.log" AND messageType >= 16'`
- 앱 재시작: `xcrun simctl terminate booted com.flirtter.zziritApp && xcrun simctl launch booted com.flirtter.zziritApp`
- API 배포: `cd ~/zzirit-api && gcloud run deploy zzirit-api --source=. --region=asia-northeast3 --allow-unauthenticated`
